({
    executeOrder66 : function(component, event, helper) {
        helper.executeOrder66(component);
	},
    
    reset : function(component, event, helper){
        helper.reset(component);
    }
})