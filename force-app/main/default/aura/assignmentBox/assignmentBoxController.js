({
	getEligibleUsers : function(component, event, helper) {
		helper.getEligibleUsers(component);	
	},
    
    saveAssignment : function(component, event, helper){
        helper.saveAssignment(component);
    },
    
    handleChange : function(component, event, helper){
		helper.handleChange(component);
    }
})